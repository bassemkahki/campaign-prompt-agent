---
phase: 03-motion-multimodal-video
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [src/services/engineer.ts, tests/prompts.test.ts]
autonomous: true
requirements: [VID-01, VID-02]
must_haves:
  truths:
    - "Prompt uses Hybrid Context + Action Delta formula"
    - "Natural language motion (Zoom, Pan, Tilt) is mapped to explicit Seedance flags (-zoom, -pan, -tilt)"
    - "Motion intensity is included as -motion flag"
  artifacts:
    - path: "src/services/engineer.ts"
      contains: "-motion"
---

<objective>
Implement the core video prompt generation logic, including the Hybrid Context + Action Delta formula and the mapping of cinematography descriptions to explicit Seedance 2.0 command flags.

**As a** creative director, **I want to** use natural language to describe camera moves, **so that** the agent generates precise technical flags for the video model.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-multimodal-video/03-01-SUMMARY.md
@.planning/phases/03-motion-multimodal-video/03-RESEARCH.md
@.planning/phases/03-motion-multimodal-video/03-CONTEXT.md
@src/services/engineer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for Hybrid Prompt and Command Mapping</name>
  <files>tests/prompts.test.ts</files>
  <behavior>
    - Test that `generateSeedance` includes core visual markers (Subject, Environment, Style, Mood).
    - Test that `generateSeedance` includes the `actionDelta` (or `pose`).
    - Test that "Zoom In" in `motionDirection` maps to `-zoom` flag.
    - Test that "Pan Left" in `motionDirection` maps to `-pan` flag.
    - Test that `motionIntensity: 7` maps to `-motion 7`.
  </behavior>
  <action>
    Expand the `generateSeedance` tests in `tests/prompts.test.ts` to verify the presence of identity markers, action delta, and technical command flags per D-03 and D-04.
  </action>
  <verify>
    <automated>npx vitest tests/prompts.test.ts --run</automated>
  </verify>
  <done>Tests fail as logic is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Hybrid Formula and Motion Mapping</name>
  <files>src/services/engineer.ts</files>
  <action>
    Update `generateSeedance` in `PromptEngineerService`:
    1. **Visual Identity**: Extract subject, environment, visualStyle, and mood. Format as a concise summary.
    2. **Action Delta**: Use `shot.actionDelta` || `shot.pose`.
    3. **Command Mapping**:
       - Map `motionIntensity` to `-motion ${val}`.
       - Scan `motionDirection` for keywords (zoom, pan, tilt) and map to hyphenated flags.
       - **Dynamic Scaling**: Scale the flag values (e.g., `-zoom`) proportionally to the `motionIntensity` (e.g., if intensity is 8, zoom might be 8; if intensity is 2, zoom might be 2) to ensure consistency and avoid clipping.
       - If "Dolly In" or "Zoom In", use `-zoom`. If "Pan", use `-pan`. If "Tilt", use `-tilt`.
  </action>
  <verify>
    <automated>npx vitest tests/prompts.test.ts --run</automated>
  </verify>
  <done>All prompt logic tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Logic | Prompt engineering formula |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | Prompt Injection | mitigate | Sanitize user-provided `actionDelta` and `motionDirection` to ensure they don't break the `-flag` structure. |
</threat_model>

<verification>
Ensure all prompt generation tests pass and the output format matches Seedance 2.0 requirements.
</verification>

<success_criteria>
- Video prompts include hyphenated command flags.
- Prompts follow the Hybrid Context + Action Delta formula.
</success_criteria>

<output>
Create `.planning/phases/03-motion-multimodal-video/03-02-SUMMARY.md` when done
</output>
