---
phase: 03-motion-multimodal-video
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/mcp/server.ts, tests/prompts.test.ts]
autonomous: true
requirements: [VID-01, VID-02, VID-03]
must_haves:
  truths:
    - "User can call generate_seedance_prompt via MCP"
    - "MCP tool returns the generated Seedance 2.0 prompt"
  artifacts:
    - path: "src/mcp/server.ts"
      contains: "generate_seedance_prompt"
---

<objective>
Expose the Seedance 2.0 prompt generation capability as a Model Context Protocol (MCP) tool and perform final validation of the integrated video generation pipeline.

**As a** creative director, **I want to** generate video prompts directly from my chat interface, **so that** I can iterate on motion ideas quickly.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-multimodal-video/03-02-SUMMARY.md
@.planning/phases/03-motion-multimodal-video/03-CONTEXT.md
@src/mcp/server.ts
@tests/prompts.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register generate_seedance_prompt tool</name>
  <files>src/mcp/server.ts</files>
  <action>
    1. Update `setupHandlers` in `CampaignAgentServer`:
       - Add `generate_seedance_prompt` to the list of tools in the `ListToolsRequestSchema` handler.
       - Include `briefId` and `shotId` as required string arguments.
    2. Add the tool implementation in the `CallToolRequestSchema` handler:
       - Fetch the brief using `storageService.getBrief(briefId)`.
       - Call `engineerService.generateSeedance(brief, shotId)`.
       - Return the generated prompt as a text content response.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>MCP tool registered and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Integrated Validation Tests</name>
  <files>tests/prompts.test.ts</files>
  <action>
    Add a final set of integration tests in `tests/prompts.test.ts` that simulate the full flow from a `CreativeBrief` with motion metadata to a complete Seedance 2.0 prompt, verifying all Phase 3 success criteria (Identity Lock, @ImageN, Motion Energy).
  </action>
  <verify>
    <automated>npx vitest tests/prompts.test.ts --run</automated>
  </verify>
  <done>All Phase 3 requirements validated via tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → MCP Server | Tool execution requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | generate_seedance_prompt | mitigate | Ensure storage access is efficient and tool execution doesn't hang on invalid shot IDs (handled by PromptEngineerService errors). |
</threat_model>

<verification>
Verify that all tests pass and the MCP tool is visible to clients (e.g., via `ping` or manual listing if possible).
</verification>

<success_criteria>
- `generate_seedance_prompt` tool is available via MCP.
- End-to-end generation of consistent video prompts is successful.
</success_criteria>

<output>
Create `.planning/phases/03-motion-multimodal-video/03-03-SUMMARY.md` when done
</output>
