---
phase: 03-motion-multimodal-video
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/schema/brief.ts, src/schema/prompt.ts, src/services/engineer.ts, tests/prompts.test.ts]
autonomous: true
requirements: [VID-03]
must_haves:
  truths:
    - "ShotBreakdownSchema includes motionIntensity (1-10), motionDirection, and actionDelta"
    - "PromptOutputSchema supports 'seedance-2' model"
    - "PromptEngineerService can resolve @ImageN based on shot index"
  artifacts:
    - path: "src/schema/brief.ts"
      contains: "motionIntensity: z.number().min(1).max(10)"
    - path: "src/services/engineer.ts"
      contains: "generateSeedance"
---

<objective>
Establish the schema and service foundation for Phase 3, enabling motion-specific metadata ingestion and 1-based @ImageN resolution logic for Seedance 2.0 prompts.

**As a** creative director, **I want to** specify motion parameters for my shots, **so that** I can generate cinematic video prompts that are perfectly synced with my source images.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-motion-multimodal-video/03-RESEARCH.md
@.planning/phases/03-motion-multimodal-video/03-CONTEXT.md
@src/schema/brief.ts
@src/schema/prompt.ts
@src/services/engineer.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing test for @ImageN resolution</name>
  <files>tests/prompts.test.ts</files>
  <behavior>
    - Test that generateSeedance correctly identifies the 1-based index of a shot in the brief.
    - Example: First shot (index 0) should produce a prompt starting with "@Image1".
    - Example: Second shot (index 1) should produce a prompt starting with "@Image2".
  </behavior>
  <action>
    Add a new test case to 'PromptEngineerService' describe block in `tests/prompts.test.ts` for `generateSeedance`.
    Create a mock brief with at least two shots and verify that the generated prompt starts with the correct @ImageN reference per VID-03 and D-02.
  </action>
  <verify>
    <automated>npx vitest tests/prompts.test.ts --run</automated>
  </verify>
  <done>Test fails as generateSeedance is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update Schemas for Motion Support</name>
  <files>src/schema/brief.ts, src/schema/prompt.ts</files>
  <action>
    1. Update `src/schema/brief.ts`: Add `motionIntensity` (z.number().min(1).max(10)), `motionDirection` (z.string().optional()), and `actionDelta` (z.string().optional()) to `ShotBreakdownSchema` per D-01.
    2. Update `src/schema/prompt.ts`: Add "seedance-2" to the model enum in `PromptOutputSchema`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Schemas updated and project compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Skeleton and @ImageN Resolution</name>
  <files>src/services/engineer.ts</files>
  <action>
    Implement `generateSeedance(brief: CreativeBrief, shotId: string): PromptOutput` in `PromptEngineerService`.
    Logic:
    1. Find the shot and its index (0-based) in `brief.shotBreakdowns`.
    2. If not found, throw an error.
    3. Generate the @ImageN reference using `index + 1`.
    4. Return a partial `PromptOutput` with model "seedance-2" and prompt starting with the @ImageN reference.
  </action>
  <verify>
    <automated>npx vitest tests/prompts.test.ts --run</automated>
  </verify>
  <done>Task 1 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User provides motion metadata in campaign docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | ShotBreakdownSchema | mitigate | Strict Zod validation for `motionIntensity` (1-10 range). |
| T-03-02 | Information Disclosure | Error handling | mitigate | Ensure shotId not found errors don't leak internal brief structure beyond the ID itself. |
</threat_model>

<verification>
Verify that the project compiles and the basic @ImageN resolution test passes.
</verification>

<success_criteria>
- ShotBreakdownSchema supports motion fields.
- PromptEngineerService can resolve the correct @ImageN index.
</success_criteria>

<output>
Create `.planning/phases/03-motion-multimodal-video/03-01-SUMMARY.md` when done
</output>
