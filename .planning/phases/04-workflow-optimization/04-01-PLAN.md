# Plan 04-01: Workflow Optimization

## Goal
Ensure production readiness and input validation.

## Requirements
- WORK-01: Input Checklist Generation

## Proposed Changes
- Create `ValidationService` for brief checking.
- Update `ShotBreakdownSchema` with `soulId`.
- Add `check_campaign_readiness` tool to MCP server.
- Inject warnings into generation tools if brief is incomplete.
