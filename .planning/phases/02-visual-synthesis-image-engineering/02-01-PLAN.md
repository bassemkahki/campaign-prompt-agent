---
phase: 02-visual-synthesis-image-engineering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/schema/brief.ts, src/services/synthesis.ts, src/mcp/server.ts, tests/synthesis.test.ts]
autonomous: true
requirements: [ANAL-02]
must_haves:
  truths:
    - "Ingested briefs contain a 'shotBreakdowns' array with structured visual data"
    - "Extraction logic identifies subject, outfit, pose, and location for each shot (per D-01)"
  artifacts:
    - path: "src/schema/brief.ts"
      contains: "shotBreakdowns"
    - path: "src/services/synthesis.ts"
      provides: "SynthesisService with extractBrief method"
---

<objective>
Enable the extraction of structured shot breakdowns from campaign documents.

Purpose: Provide the foundation for specific image generation by identifying individual shots mentioned in creative briefs.
Output: Updated CreativeBriefSchema and a dedicated SynthesisService.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-visual-synthesis-image-engineering/02-RESEARCH.md
@.planning/phases/02-visual-synthesis-image-engineering/02-CONTEXT.md
@src/schema/brief.ts
@src/mcp/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend CreativeBriefSchema</name>
  <files>src/schema/brief.ts</files>
  <action>
    Add a `shotBreakdowns` field to `CreativeBriefSchema`. 
    Each shot should be an object containing:
    - `id`: string (unique identifier)
    - `description`: string (raw description)
    - `subject`: string
    - `outfit`: string
    - `pose`: string
    - `location`: string
    - `shotType`: string (e.g., MCU, Wide)
    - `lightingOverride`: string (optional)
  </action>
  <verify>
    Check file contains `shotBreakdowns: z.array(`
  </verify>
  <done>Schema updated and exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SynthesisService</name>
  <files>src/services/synthesis.ts, tests/synthesis.test.ts</files>
  <behavior>
    - `extractBrief(markdown: string)` should use Vercel AI SDK to return a validated `CreativeBrief`.
    - It must specifically look for shot lists or breakdowns in the text (D-01).
    - If no shots are found, it should return an empty array for `shotBreakdowns`.
  </behavior>
  <action>
    Create `SynthesisService` class. Migrate the extraction logic from `src/mcp/server.ts` into this service.
    Configure the prompt to emphasize extracting individual shots with subject/outfit/pose details.
  </action>
  <verify>
    <automated>npm test tests/synthesis.test.ts</automated>
  </verify>
  <done>Service implemented and tested with mock markdown.</done>
</task>

<task type="auto">
  <name>Task 3: Update MCP Server to use SynthesisService</name>
  <files>src/mcp/server.ts</files>
  <action>
    Refactor `ingest_campaign_doc` tool in `server.ts` to use `SynthesisService.extractBrief`.
    Remove the inline `generateText` call for extraction.
    Ensure the response includes the number of shots extracted.
  </action>
  <verify>
    Run `npm test tests/ingestion.test.ts` (update test if needed to handle new schema).
  </verify>
  <done>MCP server updated and integrated with new synthesis logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docling → synthesis | Raw document text is processed by LLM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .campaign/briefs/*.json | mitigate | Local storage only, respect .gitignore |
| T-02-02 | Tampering | brief extraction prompt | mitigate | Static prompt templates, no user-controlled injection into prompt instructions |
</threat_model>

<verification>
Verify that ingesting a document now produces a JSON brief with a populated `shotBreakdowns` array.
</verification>

<success_criteria>
User can ingest a campaign document and receive a confirmation of the number of shots identified.
</success_criteria>

<output>
Create `.planning/phases/02-visual-synthesis-image-engineering/02-01-SUMMARY.md` when done
</output>
