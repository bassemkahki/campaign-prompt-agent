---
phase: 02-visual-synthesis-image-engineering
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [src/schema/prompt.ts, src/services/engineer.ts, src/mcp/server.ts, tests/prompts.test.ts]
autonomous: true
requirements: [IMG-01, IMG-03]
must_haves:
  truths:
    - "Soul V2 prompts follow the 'Subject + Outfit + Pose + Camera' formula"
    - "Prompts utilize the AOC (Action, Object, Context) structure (per D-02)"
    - "Campaign-wide visual style and mood are injected into every prompt (IMG-03)"
  artifacts:
    - path: "src/services/engineer.ts"
      provides: "SoulV2Engineer"
    - path: "src/mcp/server.ts"
      contains: "generate_soul_v2_prompt"
---

<objective>
Implement the stylized prompt generation for Soul V2.

Purpose: Transform extracted shot data into high-fidelity fashion/editorial prompts following the Soul V2 formula.
Output: PromptSchema, SoulV2Engineer, and a new MCP tool.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-visual-synthesis-image-engineering/02-RESEARCH.md
@.planning/phases/02-visual-synthesis-image-engineering/02-CONTEXT.md
@src/schema/brief.ts
@src/services/synthesis.ts
@src/mcp/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define PromptSchema</name>
  <files>src/schema/prompt.ts</files>
  <action>
    Create a `PromptOutputSchema` using Zod.
    It should include:
    - `shotId`: string
    - `model`: enum ["soul-v2", "soul-cinema"]
    - `prompt`: string
    - `negativePrompt`: string (optional)
    - `metadata`: object (store seeds or other params if needed)
  </action>
  <verify>
    Check file exists and exports the schema.
  </verify>
  <done>Prompt schema defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SoulV2Engineer</name>
  <files>src/services/engineer.ts, tests/prompts.test.ts</files>
  <behavior>
    - `generateSoulV2(brief: CreativeBrief, shotId: string)` should find the shot and return a structured prompt.
    - Formula: [Subject] wearing [Outfit], [Pose/Action], shot on [Camera] with [Lighting].
    - Injects `brief.artDirection.visualStyle` and `brief.mood` into the prompt (IMG-03).
    - Uses AOC framework logic: Action = pose, Object = subject+outfit, Context = camera+lighting+style.
  </behavior>
  <action>
    Create `PromptEngineerService` class in `src/services/engineer.ts`.
    Implement `generateSoulV2` method.
    Handle cases where shot details might be missing with reasonable defaults.
  </action>
  <verify>
    <automated>npm test tests/prompts.test.ts</automated>
  </verify>
  <done>Soul V2 engineering logic implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Register Soul V2 Tool in MCP Server</name>
  <files>src/mcp/server.ts</files>
  <action>
    Add `generate_soul_v2_prompt` tool to `server.ts` (D-04).
    Inputs: `briefId` (string), `shotId` (string).
    Logic:
    1. Load brief from `StorageService`.
    2. Call `PromptEngineerService.generateSoulV2`.
    3. Return the generated prompt as text.
  </action>
  <verify>
    Run `npm test tests/ingestion.test.ts` (or add a new MCP tool test).
  </verify>
  <done>MCP tool exposed and functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storage → Engineer | Data retrieved from local JSON files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | prompt formula injection | mitigate | Use fixed formula strings with template literal interpolation, no direct eval. |
</threat_model>

<verification>
Verify that calling the tool with a valid brief and shot ID returns a prompt that follows the Soul V2 formula and includes the campaign style.
</verification>

<success_criteria>
User can generate stylized Soul V2 prompts for any extracted shot in their campaign.
</success_criteria>

<output>
Create `.planning/phases/02-visual-synthesis-image-engineering/02-02-SUMMARY.md` when done
</output>
