---
phase: 01-foundation-analysis
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [src/mcp/server.ts, src/ingestion/docling.ts, src/services/storage.ts, src/schema/brief.ts, tests/ingestion.test.ts, tests/storage.test.ts, .campaign/briefs/.keep]
autonomous: true
requirements: [ANAL-01, WORK-02]

must_haves:
  truths:
    - "A campaign document (PDF/PPTX) can be converted to Markdown via Docling"
    - "Markdown documents (.md) can be ingested directly without conversion"
    - "A creative brief is extracted from Markdown using structured AI output"
    - "Extracted briefs are persisted as JSON in .campaign/briefs/"
    - "The MCP server exposes an 'ingest_campaign_doc' tool"
  artifacts:
    - path: "src/ingestion/docling.ts"
      provides: "Wrapper for Docling CLI to parse documents, and direct reader for .md"
      min_lines: 30
    - path: "src/services/storage.ts"
      provides: "Local persistence for briefs and sources"
      min_lines: 30
    - path: "src/schema/brief.ts"
      provides: "Zod schema for the Canonical Creative Brief"
      min_lines: 40
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/ingestion/docling.ts"
      via: "Tool handler calling IngestionService"
      pattern: "IngestionService\\.parse"
    - from: "src/mcp/server.ts"
      to: "src/services/storage.ts"
      via: "Tool handler calling StorageService"
      pattern: "StorageService\\.saveBrief"
    - from: "src/ingestion/docling.ts"
      to: "child_process"
      via: "execSync call to docling CLI"
      pattern: "execSync.*docling"
---

# Plan 03: Creative Ingestion & Storage

<objective>
**As a** creative professional, **I want to** ingest campaign documents, **so that** the agent can analyze my brand and visual requirements.

Purpose: Completes the ingestion vertical slice.
Output: An MCP tool that parses documents into structured JSON briefs.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-analysis/01-CONTEXT.md
@.planning/phases/01-foundation-analysis/01-RESEARCH.md
@.planning/phases/01-foundation-analysis/01-PATTERNS.md
@.planning/phases/01-foundation-analysis/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ingestion & Storage Test Base</name>
  <files>tests/ingestion.test.ts, tests/storage.test.ts</files>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-RESEARCH.md (Code Examples)
  </read_first>
  <behavior>
    - Test: Call the `ingest_campaign_doc` tool with a PDF path.
    - Test: Call the `ingest_campaign_doc` tool with a Markdown path.
    - Test: Verify storage service saves and loads JSON.
  </behavior>
  <action>
    1. Create `tests/ingestion.test.ts` for tool testing.
    2. Create `tests/storage.test.ts` to verify local persistence.
  </action>
  <acceptance_criteria>
    - `npm test tests/ingestion.test.ts` runs and fails.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Failing tests define the ingestion and storage contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Ingestion (Docling Wrapper + Direct MD) and Storage Service</name>
  <files>src/ingestion/docling.ts, src/services/storage.ts, src/schema/brief.ts, .campaign/briefs/.keep</files>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-PATTERNS.md (Docling & Storage Patterns)
  </read_first>
  <action>
    1. Implement `src/ingestion/docling.ts` (IngestionService):
       - If file extension is `.md`, read file content directly.
       - Otherwise, use `execSync` to call the `docling` CLI to convert to Markdown.
    2. Implement `src/services/storage.ts` (StorageService) to manage the `.campaign/` folder.
    3. Define `CreativeBriefSchema` in `src/schema/brief.ts` using Zod.
  </action>
  <acceptance_criteria>
    - `src/services/storage.ts` creates the `.campaign/briefs` directory.
    - `tests/storage.test.ts` PASSES.
  </acceptance_criteria>
  <verify>
    <automated>npm test tests/storage.test.ts</automated>
  </verify>
  <done>Ingestion and storage foundations are ready, supporting PDF, PPTX, and MD.</done>
</task>

<task type="auto">
  <name>Task 3: Implement ingest_campaign_doc MCP Tool</name>
  <files>src/mcp/server.ts</files>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-PATTERNS.md (AI SDK Structured Extraction)
  </read_first>
  <action>
    1. Register `ingest_campaign_doc` tool in `src/mcp/server.ts`.
    2. In the tool handler:
       a. Call `IngestionService` (src/ingestion/docling.ts) to get Markdown content (handles conversion if needed per ANAL-01).
       b. Use Vercel AI SDK to extract structured data from Markdown according to `CreativeBriefSchema`.
       c. Call `StorageService` (src/services/storage.ts) to persist the extraction to `.campaign/briefs/`.
  </action>
  <acceptance_criteria>
    - `npm test tests/ingestion.test.ts` PASSES.
  </acceptance_criteria>
  <verify>
    <automated>npm test tests/ingestion.test.ts</automated>
  </verify>
  <done>The ingestion vertical slice is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System → Docling | Malicious documents passed to CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | Path Arguments | mitigate | Sanitize all file paths in tool arguments. |
| T-01-05 | Information Disclosure | Local Files | mitigate | Restrict file access to project directory. |
| T-01-SC | Tampering | npm/pip installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages. |
</threat_model>

<verification>
- `npm test` passes for the ingestion suite.
</verification>

<success_criteria>
- User can call `ingest_campaign_doc` and see a stored JSON brief.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-analysis/01-03-SUMMARY.md` when done
</output>
