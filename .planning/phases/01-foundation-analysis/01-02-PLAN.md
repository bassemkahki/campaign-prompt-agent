---
phase: 01-foundation-analysis
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [src/mcp/server.ts, src/services/config.ts, config.json, config.template.json, package.json]
autonomous: false
requirements: [BACK-01, BACK-02]

must_haves:
  truths:
    - "The MCP server starts over Stdio and responds to a 'ping' tool call"
    - "Project configuration is persisted in a local config.json (ignored by git)"
  artifacts:
    - path: "src/mcp/server.ts"
      provides: "MCP Server entry point using StdioServerTransport"
      min_lines: 40
    - path: "src/services/config.ts"
      provides: "Local configuration management service"
      min_lines: 30
    - path: "config.json"
      provides: "Active project configuration (local only)"
      min_lines: 5
  key_links:
    - from: "src/mcp/server.ts"
      to: "src/services/config.ts"
      via: "Dependency injection or direct import"
      pattern: "import.*ConfigService"
    - from: "src/mcp/server.ts"
      to: "@modelcontextprotocol/sdk"
      via: "Server initialization"
      pattern: "new Server"
---

# Plan 02: MCP Foundation & Config

<objective>
**As a** developer, **I want to** implement the MCP server and configuration service, **so that** I can connect the agent to a host and manage local state.

Purpose: Completes the functional MCP "Walking Skeleton".
Output: A working MCP server responding to tools and a local config system.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-analysis/01-CONTEXT.md
@.planning/phases/01-foundation-analysis/01-PATTERNS.md
@.planning/phases/01-foundation-analysis/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Config Service and Template</name>
  <files>src/services/config.ts, config.template.json, config.json</files>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-CONTEXT.md (D-06, D-07)
  </read_first>
  <behavior>
    - Test: Config service loads defaults if config.json is missing.
    - Test: Config service loads values from config.json if present.
  </behavior>
  <action>
    1. Create `src/services/config.ts` to manage `config.json`.
    2. Create `config.template.json` with placeholders for API keys (e.g., ANTHROPIC_API_KEY, OPENAI_API_KEY).
    3. Ensure `config.json` is created locally but ignored by git (per D-07).
  </action>
  <acceptance_criteria>
    - Config service correctly identifies environment and project settings.
  </acceptance_criteria>
  <verify>
    <automated>npm test tests/setup.test.ts</automated>
  </verify>
  <done>Configuration management is established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Stdio MCP Server</name>
  <files>src/mcp/server.ts, package.json</files>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-PATTERNS.md (MCP Server Pattern)
  </read_first>
  <behavior>
    - Test: Server connects and lists tools.
    - Test: 'ping' tool returns 'pong'.
  </behavior>
  <action>
    1. Implement `src/mcp/server.ts` using `@modelcontextprotocol/sdk`.
    2. Register a 'ping' tool that reads from `ConfigService`.
    3. Update `package.json` with a `start` script using `tsx`.
  </action>
  <acceptance_criteria>
    - `npm test tests/mcp.test.ts` PASSES.
  </acceptance_criteria>
  <verify>
    <automated>npm test tests/mcp.test.ts</automated>
  </verify>
  <done>MCP server is functional over Stdio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual MCP Host Verification</name>
  <read_first>
    - src/mcp/server.ts
  </read_first>
  <action>
    Install the agent in a local MCP host (Claude Code or Gemini CLI) to verify end-to-end connectivity.
    Steps:
    1. For Claude Code: Run `claude mcp add campaign-agent -- node <path-to-dist>/server.js` (or use tsx).
    2. Ask the host: "List the tools available in the campaign-agent."
    3. Ask the host: "Run the ping tool from the campaign-agent."
  </action>
  <acceptance_criteria>
    - Host successfully discovers the 'ping' tool.
    - Host successfully calls 'ping' and receives 'pong'.
  </acceptance_criteria>
  <verify>Manual confirmation of tool discovery and execution in the host.</verify>
  <done>Agent is successfully integrated with a real MCP host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → MCP Server | Untrusted tool arguments cross here via Stdio. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Stdio Stream | mitigate | Use standard MCP SDK transport. |
| T-01-02 | Information Disclosure | config.json | mitigate | Ensure .gitignore coverage. |
</threat_model>

<verification>
- `npm test` passes.
- MCP host confirms tool discovery.
</verification>

<success_criteria>
- The 'ping' tool returns a valid response including project name.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-analysis/01-02-SUMMARY.md` when done
</output>
