---
phase: 01-foundation-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, vitest.config.ts, .gitignore, tests/mcp.test.ts, tests/setup.test.ts]
autonomous: false
requirements: [BACK-01]

must_haves:
  truths:
    - "The project has a valid package.json and tsconfig.json"
    - "A Vitest test suite exists and can run an MCP connection test"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
---

# Plan 01: Environment & Scaffold

<objective>
**As a** developer, **I want to** initialize the project environment and test infrastructure, **so that** I have a stable and secure foundation for building the MCP server.

Purpose: Establishes the base environment and proves test connectivity.
Output: Project scaffold with validated dependencies and initial test stubs.
</objective>

<execution_context>
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/bassemelkahki/Documents/dev/skilldev/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-analysis/01-CONTEXT.md
@.planning/phases/01-foundation-analysis/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Gate</name>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-RESEARCH.md (Package Legitimacy Audit)
  </read_first>
  <action>
    Verify the following [ASSUMED] packages on their respective registries before installation:
    - @modelcontextprotocol/sdk (npm)
    - ai (npm)
    - zod (npm)
    - docling (PyPI - for future use)
    - sharp (npm)
    - zustand (npm)
    - commander (npm)
  </action>
  <acceptance_criteria>
    - User confirms package legitimacy.
  </acceptance_criteria>
  <verify>Manual check of npmjs.com/package and pypi.org/project for each library.</verify>
  <done>User approves the package list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project Scaffold and Initial Test Infrastructure</name>
  <files>package.json, tsconfig.json, vitest.config.ts, tests/mcp.test.ts, tests/setup.test.ts, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation-analysis/01-RESEARCH.md (Standard Stack)
  </read_first>
  <behavior>
    - Test: Initialize an MCP Client stub.
    - Test: Assert basic project setup (setup.test.ts).
    - Expected: Tests pass or fail predictably based on missing source files.
  </behavior>
  <action>
    1. Initialize `package.json` with dependencies from RESEARCH.md (excluding docling for now).
    2. Configure `tsconfig.json` for Node.js 20+ and ESM.
    3. Setup `vitest.config.ts`.
    4. Create `tests/mcp.test.ts` as a stub for MCP connection testing.
    5. Create `tests/setup.test.ts` to verify the environment.
    6. Ensure `.gitignore` ignores `node_modules`, `dist`, and `config.json`.
  </action>
  <acceptance_criteria>
    - `npm install` completes successfully.
    - `npm test` runs (even if connection tests fail due to missing server).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Scaffold exists and Vitest is configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Registry | Dependency installation from npm/PyPI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages. |
</threat_model>

<verification>
- `npm test` runs successfully.
- .gitignore correctly configured.
</verification>

<success_criteria>
- Project is initialized with validated dependencies.
- Test runner is functional.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-analysis/01-01-SUMMARY.md` when done
</output>
