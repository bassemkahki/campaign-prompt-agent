{
  "projectName": "Campaign Prompt Agent",
  "storageRoot": ".campaign",
  "_note": "No API keys required. The agent runs inside your CLI (Claude Code / Gemini CLI) and uses that host's own model for synthesis. config.json is optional — defaults are used if it is absent."
}
